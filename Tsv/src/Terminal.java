/**
 * An enum with the different statistics that need to be computed.
 * 
 * @author Tian Low ttl2132
 *
 */
public enum Terminal {
	ALLSAME, COUNT, MIN, MAX, SUM, STATS, AVERAGE, STANDARD_DEVIATION;
}
